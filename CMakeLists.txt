# This file was generated by Jucer2Reprojucer from "GPSynth.jucer"

cmake_minimum_required(VERSION 3.4)

project("GPSynth")

# variables
set(VST_BINARY_LOCATION "" CACHE PATH "Location for VST deployment")

if (VST_BINARY_LOCATION)
	set(EXPORTER_CONFIG_STRING_VST_DEPLOY VST_BINARY_LOCATION ${VST_BINARY_LOCATION})
	message(${EXPORTER_CONFIG_STRING_VST_DEPLOY})
endif()

list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/libs/FRUT/cmake")
include(Reprojucer)


set(GPSynth_jucer_FILE
  "${CMAKE_CURRENT_LIST_DIR}/GPSynth.jucer"
)


# 3rd party
add_subdirectory(libs/GPOsc)


jucer_project_begin(
  JUCER_VERSION "5.3.2"
  PROJECT_FILE "${GPSynth_jucer_FILE}"
  PROJECT_ID "gSVXXS"
)

jucer_project_settings(
  PROJECT_NAME "GPSynth"
  PROJECT_VERSION "0.1.0"
  REPORT_JUCE_APP_USAGE ON # Required for closed source applications without an Indie or Pro JUCE license
  DISPLAY_THE_JUCE_SPLASH_SCREEN ON # Required for closed source applications without an Indie or Pro JUCE license
  PROJECT_TYPE "Audio Plug-in"
  BUNDLE_IDENTIFIER "com.yourcompany.GPSynth"
  CXX_LANGUAGE_STANDARD "C++14"
)

jucer_audio_plugin_settings(
  PLUGIN_FORMATS
    "VST"
    "AU"
    "Standalone"
  # PLUGIN_CHARACTERISTICS
  PLUGIN_NAME "GPSynth"
  PLUGIN_DESCRIPTION "GPSynth"
  PLUGIN_MANUFACTURER "yourcompany"
  PLUGIN_MANUFACTURER_CODE "Manu"
  PLUGIN_CODE "Gsvx"
  PLUGIN_IS_A_SYNTH ON
  PLUGIN_MIDI_INPUT ON
  # PLUGIN_CHANNEL_CONFIGURATIONS
  PLUGIN_AAX_IDENTIFIER "com.yourcompany.GPSynth"
  PLUGIN_AU_EXPORT_PREFIX "GPSynthAU"
  PLUGIN_AU_MAIN_TYPE "kAudioUnitType_Effect"
  PLUGIN_VST_CATEGORY "kPlugCategEffect"
  PLUGIN_VST3_CATEGORY
    "Fx"
  PLUGIN_RTAS_CATEGORY
    "ePlugInCategory_None"
  PLUGIN_AAX_CATEGORY
    "AAX_ePlugInCategory_None"
)

jucer_project_files("GPSynth/Source"
# Compile   Xcode     Binary    File
#           Resource  Resource
  x         .         .         "Source/PluginProcessor.cpp"
  .         .         .         "Source/PluginProcessor.h"
  x         .         .         "Source/PluginEditor.cpp"
  .         .         .         "Source/PluginEditor.h"
  x         .         .         "Source/GPSynthVoice.cpp"
  .         .         .         "Source/GPSynthVoice.h"
)

jucer_project_module(
  juce_audio_basics
  PATH "libs/JUCE/modules"
)

jucer_project_module(
  juce_audio_devices
  PATH "libs/JUCE/modules"
  # JUCE_USE_WINRT_MIDI
  # JUCE_ASIO
  # JUCE_WASAPI
  # JUCE_WASAPI_EXCLUSIVE
  # JUCE_DIRECTSOUND
  # JUCE_ALSA
  # JUCE_JACK
  # JUCE_BELA
  # JUCE_USE_ANDROID_OBOE
  # JUCE_USE_ANDROID_OPENSLES
  # JUCE_DISABLE_AUDIO_MIXING_WITH_OTHER_APPS
)

jucer_project_module(
  juce_audio_formats
  PATH "libs/JUCE/modules"
  # JUCE_USE_FLAC
  # JUCE_USE_OGGVORBIS
  # JUCE_USE_MP3AUDIOFORMAT
  # JUCE_USE_LAME_AUDIO_FORMAT
  # JUCE_USE_WINDOWS_MEDIA_FORMAT
)

jucer_project_module(
  juce_audio_plugin_client
  PATH "libs/JUCE/modules"
  # JUCE_VST3_CAN_REPLACE_VST2
  # JUCE_FORCE_USE_LEGACY_PARAM_IDS
  # JUCE_FORCE_LEGACY_PARAMETER_AUTOMATION_TYPE
  # JUCE_USE_STUDIO_ONE_COMPATIBLE_PARAMETERS
  # JUCE_STANDALONE_FILTER_WINDOW_USE_KIOSK_MODE
)

jucer_project_module(
  juce_audio_processors
  PATH "libs/JUCE/modules"
  # JUCE_PLUGINHOST_VST
  # JUCE_PLUGINHOST_VST3
  # JUCE_PLUGINHOST_AU
  # JUCE_PLUGINHOST_LADSPA
)

jucer_project_module(
  juce_audio_utils
  PATH "libs/JUCE/modules"
  # JUCE_USE_CDREADER
  # JUCE_USE_CDBURNER
)

jucer_project_module(
  juce_core
  PATH "libs/JUCE/modules"
  # JUCE_FORCE_DEBUG
  # JUCE_LOG_ASSERTIONS
  # JUCE_CHECK_MEMORY_LEAKS
  # JUCE_DONT_AUTOLINK_TO_WIN32_LIBRARIES
  # JUCE_INCLUDE_ZLIB_CODE
  # JUCE_USE_CURL
  # JUCE_LOAD_CURL_SYMBOLS_LAZILY
  # JUCE_CATCH_UNHANDLED_EXCEPTIONS
  # JUCE_ALLOW_STATIC_NULL_VARIABLES
  # JUCE_STRICT_REFCOUNTEDPOINTER
)

jucer_project_module(
  juce_cryptography
  PATH "libs/JUCE/modules"
)

jucer_project_module(
  juce_data_structures
  PATH "libs/JUCE/modules"
)

jucer_project_module(
  juce_events
  PATH "libs/JUCE/modules"
  # JUCE_EXECUTE_APP_SUSPEND_ON_IOS_BACKGROUND_TASK
)

jucer_project_module(
  juce_graphics
  PATH "libs/JUCE/modules"
  # JUCE_USE_COREIMAGE_LOADER
  # JUCE_USE_DIRECTWRITE
  # JUCE_DISABLE_COREGRAPHICS_FONT_SMOOTHING
)

jucer_project_module(
  juce_gui_basics
  PATH "libs/JUCE/modules"
  # JUCE_ENABLE_REPAINT_DEBUGGING
  # JUCE_USE_XRANDR
  # JUCE_USE_XINERAMA
  # JUCE_USE_XSHM
  # JUCE_USE_XRENDER
  # JUCE_USE_XCURSOR
  # JUCE_WIN_PER_MONITOR_DPI_AWARE
)

jucer_project_module(
  juce_gui_extra
  PATH "libs/JUCE/modules"
  # JUCE_WEB_BROWSER
  # JUCE_ENABLE_LIVE_CONSTANT_EDITOR
)

jucer_project_module(
  juce_opengl
  PATH "libs/JUCE/modules"
)

jucer_project_module(
  juce_video
  PATH "libs/JUCE/modules"
  # JUCE_USE_CAMERA
  # JUCE_SYNC_VIDEO_VOLUME_WITH_OS_MEDIA_VOLUME
)

jucer_export_target(
  "Visual Studio 2017"
  EXTERNAL_LIBRARIES_TO_LINK
  GPOsc_Lib
)

jucer_export_target_configuration(
  "Visual Studio 2017"
  NAME "Debug"
  DEBUG_MODE ON
  HEADER_SEARCH_PATHS
	"libs/GPOsc/include"
  ENABLE_PLUGIN_COPY_STEP ON
  ${EXPORTER_CONFIG_STRING_VST_DEPLOY}
)

jucer_export_target_configuration(
  "Visual Studio 2017"
  NAME "Release"
  DEBUG_MODE OFF
  HEADER_SEARCH_PATHS
	"libs/GPOsc/include"
  ENABLE_PLUGIN_COPY_STEP ON
  ${EXPORTER_CONFIG_STRING_VST_DEPLOY}
)


jucer_export_target_configuration(
  "Visual Studio 2017"
  NAME "RelWithDebInfo"
  DEBUG_MODE OFF
  HEADER_SEARCH_PATHS
	"libs/GPOsc/include"
  ENABLE_PLUGIN_COPY_STEP ON
  ${EXPORTER_CONFIG_STRING_VST_DEPLOY}
)


jucer_export_target(
  "Visual Studio 2015"
  EXTRA_COMPILER_FLAGS
    "/bigobj"
  EXTERNAL_LIBRARIES_TO_LINK
  GPOsc_Lib
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Debug"
  DEBUG_MODE ON
  HEADER_SEARCH_PATHS
	"libs/GPOsc/include"
  ENABLE_PLUGIN_COPY_STEP ON
  ${EXPORTER_CONFIG_STRING_VST_DEPLOY}
)

jucer_export_target_configuration(
  "Visual Studio 2015"
  NAME "Release"
  DEBUG_MODE OFF
  HEADER_SEARCH_PATHS
	"libs/GPOsc/include"
  ENABLE_PLUGIN_COPY_STEP ON
  ${EXPORTER_CONFIG_STRING_VST_DEPLOY}
)


jucer_project_end()


